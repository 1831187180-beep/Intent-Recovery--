# Podfile for OneSignal iOS SDK v5.x Integration
platform :ios, '14.0'
use_frameworks!

target 'IntentRecovery' do
  pod 'OneSignalXCFramework', '>= 5.0.0', '< 6.0'

  target 'IntentRecoveryTests' do
    inherit! :search_paths
  end
end

target 'OneSignalNotificationServiceExtension' do
  pod 'OneSignalXCFramework', '>= 5.0.0', '< 6.0'
end
